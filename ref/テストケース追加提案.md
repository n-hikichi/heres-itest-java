# テストケース追加提案書

**作成日**: 2025-11-06
**目的**: 結合テストの網羅度向上
**現在のテストケース数**: 約312ケース
**提案追加数**: 約80ケース（合計約392ケース）

---

## 目次

1. [優先度別追加提案](#優先度別追加提案)
2. [カテゴリ別追加提案](#カテゴリ別追加提案)
3. [未テストモジュールのテスト追加](#未テストモジュールのテスト追加)
4. [既存テストの拡充](#既存テストの拡充)
5. [実装優先順位](#実装優先順位)

---

## 優先度別追加提案

### 🔴 高優先度（推奨：すぐに追加）

| 追加テストID | テスト名 | 対象モジュール | 追加理由 |
|------------|---------|--------------|---------|
| **IT-011** | Telework境界値テスト | telework.js | 勤務時間の境界値未テスト |
| **IT-012** | Telework異常系テスト | telework.js | エラーハンドリング未テスト |
| **IT-013** | Geolocation境界値テスト | geolocation.js | 緯度経度の範囲外未テスト |
| **IT-014** | UserInfo境界値テスト | user-info.js | ユーザー情報の異常値未テスト |
| **IT-015** | LoginAuth多重ログインテスト | login-auth.js | セッション管理未テスト |
| **IT-016** | DB同時実行制御テスト | db-util.js | トランザクション未テスト |

### 🟡 中優先度（推奨：次フェーズで追加）

| 追加テストID | テスト名 | 対象モジュール | 追加理由 |
|------------|---------|--------------|---------|
| **IT-017** | Whereabouts結合テスト | whereabouts.js | 未テストモジュール |
| **IT-018** | PresenceStatus結合テスト | presence-status.js | 未テストモジュール |
| **IT-019** | Department結合テスト | department.js | 未テストモジュール |
| **IT-020** | PlaceList結合テスト | place-list.js | 未テストモジュール |
| **IT-021** | APIAuth結合テスト | api-auth.js | トークン管理の詳細未テスト |

### 🟢 低優先度（オプション：リソース次第）

| 追加テストID | テスト名 | 対象モジュール | 追加理由 |
|------------|---------|--------------|---------|
| **IT-022** | パフォーマンステスト | 全モジュール | 性能測定未実施 |
| **IT-023** | セキュリティテスト | 全モジュール | 脆弱性チェック未実施 |
| **IT-024** | 負荷テスト | 全モジュール | 同時アクセス未テスト |

---

## カテゴリ別追加提案

### 1. エッジケース・境界値テスト（約20ケース）

#### IT-011: Telework境界値テスト

```javascript
// ref/telework-boundary.integration.test.js

describe('IT-011: Telework境界値テスト', () => {

  test('IT-011-01: 0時0分0秒の勤務開始', async () => {
    // Given: 0時0分0秒のタイムスタンプ
    const logTime = '00:00:00'
    const logDate = '2025-11-06'

    // When: 勤務開始
    const result = await telework.startWork({
      userId: 'user001',
      companyId: 'company001',
      category: 1,
      logDate,
      logTime,
      latitude: 35.6812,
      longitude: 139.7671
    })

    // Then: 正常に登録される
    expect(result).toBeDefined()
    expect(result.error).toBe(false)
  })

  test('IT-011-02: 23時59分59秒の勤務終了', async () => {
    // Given: 23時59分59秒のタイムスタンプ
    const logTime = '23:59:59'

    // When: 勤務終了
    const result = await telework.endWork({
      userId: 'user001',
      companyId: 'company001',
      category: 2,
      logDate: '2025-11-06',
      logTime,
      latitude: 35.6812,
      longitude: 139.7671
    })

    // Then: 正常に登録される
    expect(result).toBeDefined()
  })

  test('IT-011-03: 24時間を超える連続勤務', async () => {
    // Given: 2025-11-06 09:00に開始
    await telework.startWork({
      userId: 'user001',
      companyId: 'company001',
      category: 1,
      logDate: '2025-11-06',
      logTime: '09:00:00'
    })

    // When: 翌日の10:00に終了（25時間勤務）
    const result = await telework.endWork({
      userId: 'user001',
      companyId: 'company001',
      category: 2,
      logDate: '2025-11-07',
      logTime: '10:00:00'
    })

    // Then: 正常に登録される、勤務時間が25時間と計算される
    expect(result).toBeDefined()
    // 勤務時間の計算確認
  })

  test('IT-011-04: 過去の日付での勤務登録', async () => {
    // Given: 1週間前の日付
    const pastDate = new Date()
    pastDate.setDate(pastDate.getDate() - 7)
    const logDate = pastDate.toISOString().split('T')[0]

    // When: 過去日付で勤務開始
    const result = await telework.startWork({
      userId: 'user001',
      companyId: 'company001',
      category: 1,
      logDate,
      logTime: '09:00:00'
    })

    // Then: ビジネスロジックに応じた結果
    // （許可する場合は正常、禁止する場合はエラー）
    expect(result).toBeDefined()
  })

  test('IT-011-05: 未来の日付での勤務登録（異常系）', async () => {
    // Given: 1週間後の日付
    const futureDate = new Date()
    futureDate.setDate(futureDate.getDate() + 7)
    const logDate = futureDate.toISOString().split('T')[0]

    // When: 未来日付で勤務開始
    const result = await telework.startWork({
      userId: 'user001',
      companyId: 'company001',
      category: 1,
      logDate,
      logTime: '09:00:00'
    })

    // Then: エラーが返る（または許可される場合は正常）
    // ビジネスルールに応じて検証
    expect(result).toBeDefined()
  })

})
```

#### IT-013: Geolocation境界値テスト

```javascript
// ref/geolocation-boundary.integration.test.js

describe('IT-013: Geolocation境界値テスト', () => {

  test('IT-013-01: 緯度の最大値（90度）', async () => {
    const result = await geolocation.updateGeolocationByMember(
      'company001',
      'member001',
      {
        lat: 90.0,  // 北極点
        lon: 0.0,
        msg: 'Test at North Pole',
        send_time: new Date().toISOString()
      }
    )

    expect(result.error).toBe(false)
  })

  test('IT-013-02: 緯度の最小値（-90度）', async () => {
    const result = await geolocation.updateGeolocationByMember(
      'company001',
      'member001',
      {
        lat: -90.0,  // 南極点
        lon: 0.0,
        msg: 'Test at South Pole',
        send_time: new Date().toISOString()
      }
    )

    expect(result.error).toBe(false)
  })

  test('IT-013-03: 緯度の範囲外（91度）- 異常系', async () => {
    const result = await geolocation.updateGeolocationByMember(
      'company001',
      'member001',
      {
        lat: 91.0,  // 範囲外
        lon: 0.0,
        msg: 'Invalid latitude',
        send_time: new Date().toISOString()
      }
    )

    // Then: バリデーションエラーまたは登録成功（実装による）
    expect(result).toBeDefined()
  })

  test('IT-013-04: 経度の最大値（180度）', async () => {
    const result = await geolocation.updateGeolocationByMember(
      'company001',
      'member001',
      {
        lat: 0.0,
        lon: 180.0,  // 日付変更線
        msg: 'Test at International Date Line',
        send_time: new Date().toISOString()
      }
    )

    expect(result.error).toBe(false)
  })

  test('IT-013-05: 経度の最小値（-180度）', async () => {
    const result = await geolocation.updateGeolocationByMember(
      'company001',
      'member001',
      {
        lat: 0.0,
        lon: -180.0,
        msg: 'Test at -180',
        send_time: new Date().toISOString()
      }
    )

    expect(result.error).toBe(false)
  })

  test('IT-013-06: NULL位置情報の処理', async () => {
    const result = await geolocation.updateGeolocationByMember(
      'company001',
      'member001',
      {
        lat: null,
        lon: null,
        msg: 'Position unknown',
        send_time: new Date().toISOString()
      }
    )

    expect(result).toBeDefined()
  })

  test('IT-013-07: 位置情報の精度テスト（小数点以下10桁）', async () => {
    const result = await geolocation.updateGeolocationByMember(
      'company001',
      'member001',
      {
        lat: 35.6812345678,  // 高精度
        lon: 139.7671234567,
        msg: 'High precision location',
        send_time: new Date().toISOString()
      }
    )

    expect(result.error).toBe(false)

    // 取得して精度が保たれているか確認
    const getResult = await geolocation.getGeolocationByMember('company001', 'member001')
    // 精度の確認
  })

})
```

---

### 2. 異常系・エラーハンドリングテスト（約15ケース）

#### IT-012: Telework異常系テスト

```javascript
// ref/telework-error.integration.test.js

describe('IT-012: Telework異常系テスト', () => {

  test('IT-012-01: 存在しないユーザーIDで勤務開始', async () => {
    const result = await telework.startWork({
      userId: 'nonexistent_user',
      companyId: 'company001',
      category: 1,
      logDate: '2025-11-06',
      logTime: '09:00:00'
    })

    // エラーが返る
    expect(result).toBeDefined()
    // expect(result.error).toBe(true)  // 実装による
  })

  test('IT-012-02: 重複した勤務開始（2回連続で開始）', async () => {
    // Given: 既に勤務開始済み
    await telework.startWork({
      userId: 'user001',
      companyId: 'company001',
      category: 1,
      logDate: '2025-11-06',
      logTime: '09:00:00'
    })

    // When: 再度勤務開始
    const result = await telework.startWork({
      userId: 'user001',
      companyId: 'company001',
      category: 1,
      logDate: '2025-11-06',
      logTime: '09:30:00'
    })

    // Then: エラーまたは上書き（ビジネスロジックによる）
    expect(result).toBeDefined()
  })

  test('IT-012-03: 勤務開始前に勤務終了', async () => {
    // Given: 勤務開始していない状態

    // When: 勤務終了を試行
    const result = await telework.endWork({
      userId: 'user001',
      companyId: 'company001',
      category: 2,
      logDate: '2025-11-06',
      logTime: '18:00:00'
    })

    // Then: エラーまたは無視（ビジネスロジックによる）
    expect(result).toBeDefined()
  })

  test('IT-012-04: 不正なcategory値', async () => {
    const result = await telework.startWork({
      userId: 'user001',
      companyId: 'company001',
      category: 999,  // 不正な値
      logDate: '2025-11-06',
      logTime: '09:00:00'
    })

    expect(result).toBeDefined()
  })

  test('IT-012-05: 必須パラメータ欠落（userIdなし）', async () => {
    const result = await telework.startWork({
      // userId: 'user001',  // 欠落
      companyId: 'company001',
      category: 1,
      logDate: '2025-11-06',
      logTime: '09:00:00'
    })

    // エラーが返る
    expect(result).toBeDefined()
  })

  test('IT-012-06: 不正な日付フォーマット', async () => {
    const result = await telework.startWork({
      userId: 'user001',
      companyId: 'company001',
      category: 1,
      logDate: '2025/11/06',  // YYYY-MM-DD形式ではない
      logTime: '09:00:00'
    })

    expect(result).toBeDefined()
  })

  test('IT-012-07: SQLインジェクション対策確認', async () => {
    const result = await telework.startWork({
      userId: "user001' OR '1'='1",  // SQLインジェクション試行
      companyId: 'company001',
      category: 1,
      logDate: '2025-11-06',
      logTime: '09:00:00'
    })

    // パラメータ化クエリで保護されている
    expect(result).toBeDefined()
  })

})
```

---

### 3. 未テストモジュールのテスト追加（約30ケース）

#### IT-017: Whereabouts結合テスト

```javascript
// ref/whereabouts.integration.test.js

const whereabouts = require('../models/whereabouts')

describe('IT-017: Whereabouts結合テスト', () => {

  test('IT-017-01: 行先情報の登録', async () => {
    // Given: ユーザーと行先情報
    const userId = 'user001'
    const companyId = 'company001'
    const whereaboutsData = {
      destination: '客先訪問',
      startTime: '10:00',
      endTime: '15:00',
      remarks: '〇〇社訪問'
    }

    // When: 行先情報を登録
    const result = await whereabouts.register(userId, companyId, whereaboutsData)

    // Then: 正常に登録される
    expect(result).toBeDefined()
    expect(result.error).toBe(false)
  })

  test('IT-017-02: 行先情報の取得', async () => {
    // Given: 登録済みの行先情報

    // When: 行先情報を取得
    const result = await whereabouts.get('user001', 'company001')

    // Then: 登録した情報が取得できる
    expect(result).toBeDefined()
    expect(result.destination).toBeDefined()
  })

  test('IT-017-03: 行先情報の更新', async () => {
    // Given: 登録済みの行先情報

    // When: 行先情報を更新
    const updateData = {
      destination: '在宅勤務',
      startTime: '09:00',
      endTime: '18:00'
    }
    const result = await whereabouts.update('user001', 'company001', updateData)

    // Then: 正常に更新される
    expect(result.error).toBe(false)
  })

  test('IT-017-04: 行先情報の削除', async () => {
    // Given: 登録済みの行先情報

    // When: 行先情報を削除
    const result = await whereabouts.delete('user001', 'company001')

    // Then: 正常に削除される
    expect(result.error).toBe(false)
  })

  test('IT-017-05: 会社全体の行先情報取得', async () => {
    // Given: 複数ユーザーの行先情報が登録されている

    // When: 会社全体の行先情報を取得
    const result = await whereabouts.getByCompany('company001')

    // Then: 全ユーザーの行先情報が取得できる
    expect(result).toBeDefined()
    expect(Array.isArray(result)).toBe(true)
  })

})
```

#### IT-018: PresenceStatus結合テスト

```javascript
// ref/presence-status.integration.test.js

const presenceStatus = require('../models/presence-status')

describe('IT-018: PresenceStatus結合テスト', () => {

  test('IT-018-01: 在席ステータスの設定（在席）', async () => {
    const result = await presenceStatus.setStatus(
      'user001',
      'company001',
      {
        status: 'present',  // 在席
        message: 'デスクにいます'
      }
    )

    expect(result.error).toBe(false)
  })

  test('IT-018-02: 在席ステータスの設定（離席）', async () => {
    const result = await presenceStatus.setStatus(
      'user001',
      'company001',
      {
        status: 'away',  // 離席
        message: '会議中'
      }
    )

    expect(result.error).toBe(false)
  })

  test('IT-018-03: 在席ステータスの取得', async () => {
    const result = await presenceStatus.getStatus('user001', 'company001')

    expect(result).toBeDefined()
    expect(result.status).toBeDefined()
  })

  test('IT-018-04: 部署全体の在席状況取得', async () => {
    const result = await presenceStatus.getByDepartment('company001', 'section001')

    expect(result).toBeDefined()
    expect(Array.isArray(result)).toBe(true)
  })

})
```

#### IT-019: Department結合テスト

```javascript
// ref/department.integration.test.js

const department = require('../models/department')

describe('IT-019: Department結合テスト', () => {

  test('IT-019-01: 部署一覧の取得', async () => {
    const result = await department.getList('company001')

    expect(result).toBeDefined()
    expect(Array.isArray(result)).toBe(true)
  })

  test('IT-019-02: 特定部署の情報取得', async () => {
    const result = await department.get('company001', 'section001')

    expect(result).toBeDefined()
    expect(result.section_name).toBeDefined()
  })

  test('IT-019-03: 部署に所属するユーザー一覧取得', async () => {
    const result = await department.getUsers('company001', 'section001')

    expect(result).toBeDefined()
    expect(Array.isArray(result)).toBe(true)
  })

})
```

---

### 4. 統合シナリオテスト（約10ケース）

#### IT-025: 複合シナリオテスト

```javascript
// ref/complex-scenario.integration.test.js

describe('IT-025: 複合シナリオテスト', () => {

  test('IT-025-01: 1日の勤務フル フロー', async () => {
    // 1. ログイン
    const loginResult = await loginAuth.authenticate('test@example.com', 'test123')
    expect(loginResult.token).toBeDefined()

    // 2. 勤務開始
    const startResult = await telework.startWork({
      userId: loginResult.user_id,
      companyId: loginResult.company_id,
      category: 1,
      logDate: '2025-11-06',
      logTime: '09:00:00',
      latitude: 35.6812,
      longitude: 139.7671
    })
    expect(startResult.error).toBe(false)

    // 3. 位置情報更新
    await geolocation.updateGeolocationByMember(
      loginResult.company_id,
      loginResult.staff_id,
      {
        lat: 35.6812,
        lon: 139.7671,
        msg: '勤務中',
        send_time: new Date().toISOString()
      }
    )

    // 4. 休憩開始
    await telework.startBreak({...})

    // 5. 休憩終了
    await telework.endBreak({...})

    // 6. 勤務終了
    const endResult = await telework.endWork({
      userId: loginResult.user_id,
      companyId: loginResult.company_id,
      category: 2,
      logDate: '2025-11-06',
      logTime: '18:00:00'
    })
    expect(endResult.error).toBe(false)

    // 7. 勤務実績確認
    // ...
  })

  test('IT-025-02: 複数ユーザーの同時勤務開始', async () => {
    // 複数ユーザーが同時に勤務開始
    const users = ['user001', 'user002', 'user003']
    const promises = users.map(userId =>
      telework.startWork({
        userId,
        companyId: 'company001',
        category: 1,
        logDate: '2025-11-06',
        logTime: '09:00:00'
      })
    )

    const results = await Promise.all(promises)

    // 全ユーザーが正常に登録される
    results.forEach(result => {
      expect(result.error).toBe(false)
    })
  })

})
```

---

### 5. パフォーマンステスト（約5ケース）

#### IT-022: パフォーマンステスト

```javascript
// ref/performance.integration.test.js

describe('IT-022: パフォーマンステスト', () => {

  test('IT-022-01: 大量データ取得のパフォーマンス', async () => {
    const startTime = Date.now()

    // 1000件のユーザー情報を取得
    const result = await db.query('SELECT * FROM iw_usertbl LIMIT 1000')

    const endTime = Date.now()
    const duration = endTime - startTime

    // 1秒以内に完了することを確認
    expect(duration).toBeLessThan(1000)
  })

  test('IT-022-02: 同時100リクエストの処理', async () => {
    const promises = []
    for (let i = 0; i < 100; i++) {
      promises.push(
        geolocation.getGeolocationByCompany('company001')
      )
    }

    const startTime = Date.now()
    const results = await Promise.all(promises)
    const endTime = Date.now()

    // 全リクエストが成功
    expect(results.length).toBe(100)

    // 5秒以内に完了
    expect(endTime - startTime).toBeLessThan(5000)
  })

})
```

---

## 実装優先順位

### Phase 1（すぐに実装）
1. **IT-011**: Telework境界値テスト（6ケース）
2. **IT-012**: Telework異常系テスト（7ケース）
3. **IT-013**: Geolocation境界値テスト（7ケース）
4. **IT-014**: UserInfo境界値テスト（5ケース）

**小計**: 25ケース（1-2日で実装可能）

### Phase 2（次フェーズ）
5. **IT-015**: LoginAuth多重ログインテスト（3ケース）
6. **IT-016**: DB同時実行制御テスト（3ケース）
7. **IT-017**: Whereabouts結合テスト（5ケース）
8. **IT-018**: PresenceStatus結合テスト（4ケース）
9. **IT-019**: Department結合テスト（3ケース）
10. **IT-020**: PlaceList結合テスト（3ケース）

**小計**: 21ケース（2-3日で実装可能）

### Phase 3（リソース次第）
11. **IT-021**: APIAuth結合テスト（5ケース）
12. **IT-022**: パフォーマンステスト（5ケース）
13. **IT-023**: セキュリティテスト（5ケース）
14. **IT-024**: 負荷テスト（3ケース）
15. **IT-025**: 複合シナリオテスト（10ケース）

**小計**: 28ケース（3-5日で実装可能）

---

## 期待される効果

### カバレッジ向上
- **現在**: 約312ケース
- **Phase 1後**: 約337ケース（+25ケース、+8%）
- **Phase 2後**: 約358ケース（+46ケース、+15%）
- **Phase 3後**: 約386ケース（+74ケース、+24%）

### 品質向上
- ✅ エッジケースの検出
- ✅ 異常系の網羅
- ✅ パフォーマンス問題の早期発見
- ✅ セキュリティ脆弱性の検出

---

## 次のアクション

どのPhaseから実装しますか？

1. **Phase 1のみ実装**（推奨）: 境界値・異常系テスト 25ケース
2. **Phase 1+2を実装**: 未テストモジュール含む 46ケース
3. **全Phase実装**: 完全網羅 74ケース
4. **特定のテストのみ選択**: カスタマイズ

---

**作成日**: 2025-11-06
**作成者**: Claude Code
