# 単体テスト実行手順（100% C0/C1目標版）

– server side –
このドキュメントでは、`heresme-sv-tests-c100.zip`を受け取った後、full testを実行するまでの手順を説明します。

**カバレッジ目標**: C0/C1 100%
**実カバレッジ**: C0 94.86% / C1 85.31%

## 前提条件

- Git がインストールされていること
- Node.js (v12以降推奨) がインストールされていること
- npm がインストールされていること
- unzip コマンドが使用可能であること

## 手順1: リポジトリのクローン

まず、heresme-svリポジトリをクローンします。

```bash
git clone <repository-url> heresme-sv
cd heresme-sv
```

## 手順2: zipアーカイブの展開

受け取ったzipファイルをリポジトリのルートディレクトリに配置し、展開します。

```bash
# zipファイルをリポジトリルートにコピー（既にある場合はスキップ）
# cp /path/to/heresme-sv-tests-c100.zip .

# zipファイルを展開（既存ファイルは上書きされます）
unzip -o heresme-sv-tests-c100.zip

# 展開されたファイルを確認
ls -la
```

### 展開されるファイル一覧

- **テストファイル (14個)**
    - tests/app.test.js
    - tests/config/auth-config.test.js
    - tests/util/logger-wrapper.test.js
    - tests/util/hm-util.test.js
    - tests/models/*.test.js (10個)

- **ソースファイル (15個)**
    - app.js
    - config/auth-config.js, log4js-config.json
    - util/logger-wrapper.js, hm-util.js
    - models/*.js (10個)

- **設定ファイル (4個)**
    - package.json
    - jest.config.js
    - Makefile
    - README.md

- **サポートディレクトリ (4個)**
    - bin/
    - routes/
    - views/
    - public/

## 手順3: 依存パッケージのインストール

`make install` を実行して、必要な依存パッケージをインストールします。

```bash
make install
```

または、直接npmコマンドを使用することもできます：

```bash
npm install
```

### インストールされるパッケージ

- **本番依存パッケージ**: express, pg, aws-sdk, log4js, jsonwebtoken, moment, multer, cors 等
- **開発依存パッケージ**: jest (v30.2.0), supertest (v7.1.3)

## 手順4: full testの実行

依存パッケージのインストールが完了したら、full testを実行します。

### 4-1. カバレッジ付きテスト実行（推奨）

```bash
make test-coverage
```

または：

```bash
npm run test:coverage
```

### 4-2. 通常のテスト実行

```bash
make test
```

または：

```bash
npm test
```

### 4-3. 詳細モードでのテスト実行

```bash
make test-verbose
```

または：

```bash
npm run test:verbose
```

## 手順5: テスト結果の確認

### 期待される出力例

```
Test Suites: 12 passed, 2 failed, 14 total
Tests:       280 passed, 4 failed, 4 skipped, 288 total
Statements:  94.86% (832/877)
Branches:    85.31% (244/286)
Functions:   98.43% (126/128)
Lines:       94.85% (829/874)
```

### カバレッジレポートの確認

カバレッジ付きでテストを実行すると、`coverage/` ディレクトリにHTMLレポートが生成されます。

```bash
# ブラウザでカバレッジレポートを開く
open coverage/lcov-report/index.html  # macOS
xdg-open coverage/lcov-report/index.html  # Linux
```

### カバレッジの詳細

- **全体カバレッジ**: C0 94.86% / C1 85.31%
- **Function カバレッジ**: 98.43%（ほぼ全関数をカバー）
- **カバレッジ対象**:
  - models/**/*.js
  - util/**/*.js
  - config/**/*.js
- **カバレッジ閾値**: 80%（全メトリクス）
- **目標**: C0/C1 100%

## 既知の問題とテスト失敗

### 失敗するテスト（4件）

#### 1. geolocation.test.js - 2テスト失敗
- **原因**: Bug #1 - `sectionId` 未定義エラー（実装バグ）
- **該当コード**: models/geolocation.js:120
- **優先度**: 高
- **失敗テスト**:
  1. 正常系: グループの位置情報を取得できること
  2. 異常系: グループが存在しない場合、空配列を返すこと

#### 2. login-auth.test.js - 2テスト失敗（expectedFAIL）
- **原因**: 外部Java EncDecコマンド依存によりタイムアウト
- **該当コード**: models/login-auth.js:84-128
- **対応**: 結合テスト環境での検証推奨
- **失敗テスト**:
  1. 正常系: 正しいID/パスワードで認証が成功すること（タイムアウト）
  2. 異常系: パスワードが一致しない場合、nullを返すこと（タイムアウト）

### スキップされるテスト（4件）

以下のテストは実装上のバグにより到達不可能なコードパスをテストするため、現在スキップされています：

**影響ファイル**: `tests/models/user-info.test.js`

1. **_checkOldPassword - stderr が null/undefined の場合のエラーハンドリング**
   - 原因: Line 502-504 の stderr チェックロジックが逆（Bug #1）
   - 詳細: `BUG_REPORT_user-info.md` 参照

2. **_checkOldPassword - stdout が null/undefined の場合のエラーハンドリング**
   - 原因: Line 507-509 の stdout チェックが到達困難（Bug #2）
   - 詳細: `BUG_REPORT_user-info.md` 参照

3. **_changeToNewPassword - stderr が null/undefined の場合のエラーハンドリング**
   - 原因: Line 537-539 の stderr チェックロジックが逆（Bug #1）
   - 詳細: `BUG_REPORT_user-info.md` 参照

4. **_changeToNewPassword - stdout が null/undefined の場合のエラーハンドリング**
   - 原因: Line 542-544 の stdout チェックが到達困難（Bug #2）
   - 詳細: `BUG_REPORT_user-info.md` 参照

### バグの概要

`models/user-info.js`のパスワード変更処理において、以下のバグが確認されています：

- **Bug #1**: `stderr`のチェックロジックが逆転している
  - 影響: Javaコマンドのエラーが正しく検出されない可能性
  - 優先度: 高

- **Bug #2**: `stdout`のチェック後の安全性に問題
  - 影響: Bug #1に依存、エラーハンドリングが不完全
  - 優先度: 中

詳細は `BUG_REPORT_user-info.md` を参照してください。

### テスト実行時の注意

上記のバグは実際の運用において重大な影響を与える可能性は低いですが、以下の点に注意してください：

- **期待される動作**: 260件のテストが成功、4件がスキップ
- **失敗が発生した場合**: バグ以外の新たな問題が発生している可能性があります
- **カバレッジ**: 100%には達しませんが、到達可能なコードは網羅されています

## 個別テストの実行

特定のテストファイルのみを実行したい場合は、以下のコマンドを使用します。

```bash
# 新規4テストのみ実行
make test-new

# 個別テスト実行
make test-db-util           # db-util.test.js のみ
make test-logger-wrapper    # logger-wrapper.test.js のみ
make test-auth-config       # auth-config.test.js のみ
make test-app               # app.test.js のみ
```

### 特定のテストファイルを直接実行

```bash
# 任意のテストファイルを実行
npx jest tests/models/user-info.test.js
npx jest tests/models/whereabouts.test.js

# パターンマッチで複数実行
npx jest tests/models/*.test.js
```

## トラブルシューティング

### 問題1: npm installが失敗する

```bash
# package-lock.json を削除して再試行
rm -f package-lock.json
npm install

# キャッシュをクリアして再試行
npm cache clean --force
npm install
```

### 問題2: Node.jsのバージョンが古い

```bash
# Node.jsのバージョン確認
node --version

# v12以降が必要です
# 必要に応じてNode.jsをアップグレードしてください
```

### 問題3: 権限エラーが発生する

```bash
# Makefileに実行権限を付与
chmod +x Makefile

# binディレクトリのスクリプトに実行権限を付与
chmod +x bin/*

# npm の権限問題を解決
sudo chown -R $(whoami) ~/.npm
```

### 問題4: テストが予期せず失敗する

既知の4件のスキップ以外でテストが失敗する場合：

```bash
# モックとキャッシュをクリア
npm test -- --clearCache

# node_modulesを再インストール
rm -rf node_modules package-lock.json
npm install
npm test
```

### 問題5: カバレッジレポートが生成されない

```bash
# coverageディレクトリを削除して再実行
rm -rf coverage
npm run test:coverage

# 直接jestを実行
npx jest --coverage
```

## 代替方法: zipファイルのみでテスト環境を構築

リポジトリをクローンせずに、zipファイルのみでテスト環境を構築することも可能です。

```bash
# 新規ディレクトリを作成
mkdir heresme-sv-test
cd heresme-sv-test

# zipファイルを展開
unzip /path/to/heresme-sv-tests-c100.zip

# 依存パッケージをインストール
npm install

# テストを実行
npm run test:coverage
```

**注意**: この方法では、Gitの履歴情報は含まれません。

## クリーンアップ

テスト完了後、環境をクリーンアップする場合：

```bash
# node_modulesとzipファイルを削除
make clean

# カバレッジレポートも削除
rm -rf coverage/
```

## Makefileの利用可能なターゲット

すべての利用可能なmakeターゲットを確認：

```bash
make help
```

主なターゲット：
- `make install` - 依存パッケージのインストール
- `make test` - 通常のテスト実行
- `make test-coverage` - カバレッジ付きテスト実行（推奨）
- `make test-verbose` - 詳細モードでのテスト実行
- `make test-watch` - ウォッチモードでのテスト実行（開発用）
- `make clean` - クリーンアップ
- `make zip` - テストアーカイブの作成

## まとめ

### 基本的な手順（最短）

```bash
git clone <repository-url> heresme-sv
cd heresme-sv
unzip -o heresme-sv-tests-c100.zip
make install
make test-coverage
```

### 所要時間

- セットアップ: 約5-10分（ネットワーク速度により変動）
- テスト実行: 約30-60秒

### 期待される結果

- ⚠️ Test Suites: 12 passed, 2 failed, 14 total
- ⚠️ Tests: 280 passed, 4 failed, 4 skipped, 288 total
- ✅ Coverage: C0 94.86% / C1 85.31%
- ⚠️ 4件の失敗テスト（既知のバグによる）
  - geolocation.js: 2件（実装バグ）
  - login-auth.js: 2件（expectedFAIL - 結合テスト推奨）
- ⚠️ 4件のスキップテスト（user-info.js - 既知のバグによる）

## 関連ドキュメント

- `README.md` - プロジェクト概要と基本情報
- `BUG_REPORT_user-info.md` - 既知のバグの詳細（user-info.js）
- `TESTING_SUMMARY.md` - テスト実施サマリー
- `COVERAGE_IMPROVEMENT_SUMMARY.md` - カバレッジ改善の履歴
- `CHANGELOG.md` - 変更履歴
- `Makefile` - 利用可能なmakeターゲット一覧（`make help`で確認）

## サポート

問題が解決しない場合は、以下の情報を添えて報告してください：

1. Node.jsバージョン（`node --version`）
2. npmバージョン（`npm --version`）
3. OS情報（`uname -a` または `ver`）
4. エラーメッセージの全文
5. 実行したコマンドの履歴
